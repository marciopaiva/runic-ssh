{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "credential",
  "description": "The credential prompt window. Deliberately empty: this window invokes the application's own commands, which the ACL does not gate, and it needs nothing from the core plugins — no events, no window control, no webview API. It is the window a secret is typed into, and the shortest list is the point. ADR-0008.",
  "windows": [
    "credential"
  ],
  "permissions": []
}
